import FooterComponent from '@components/common/footer/index';
import SideMenuComponent from '@components/common/sidemenu/index';
import LoaderComponent from '@components/common/loader/index';

export { FooterComponent, SideMenuComponent, LoaderComponent };

export const CommonComponents = {
  Footer: FooterComponent,
  SideMenu: SideMenuComponent,
  Loader: LoaderComponent,
};

export default CommonComponents;
