import defaults from '@gql/Saga/defaults';
import mutations from '@gql/Saga/mutations';
import queries from '@gql/Saga/queries';
import resolvers from '@gql/Saga/resolvers';

export { defaults, mutations, queries, resolvers };

export const Saga = {
  defaults,
  mutations,
  queries,
  resolvers,
};

export default Saga;
