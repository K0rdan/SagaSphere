import defaults from '@gql/SagaList/defaults';
import mutations from '@gql/SagaList/mutations';
import queries from '@gql/SagaList/queries';
import resolvers from '@gql/SagaList/resolvers';

export { defaults, mutations, queries, resolvers };

export const SagaList = {
  defaults,
  mutations,
  queries,
  resolvers,
};

export default SagaList;
